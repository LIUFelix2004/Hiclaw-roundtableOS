# T2.2 · ChatView hiclaw 模式渲染 — Codex 指令

> 配套文档：根目录 `前端A任务清单_Codex指令.md`（A-04 Chat 对话视图原始需求）
> 分支：在 `main` 上新开 `feat/t2.2-chatview-hiclaw`
> 目标：让 `NEXT_PUBLIC_BACKEND=hiclaw` 时，ChatView 能完整渲染经 8650 桥过来的事件流

---

## 零、先读这一节：现状与清单不一致的地方

动手前请先核对，不要照搬任务清单的措辞：

**1. ChatView 不是空的，A-04 已完成约八成。**
`packages/web/src/components/chat/ChatView.tsx` 已经实现了：`task:plan` 任务卡片、
`agent:stream` 逐字流式追加、`agent:output` 经 `MessageBubble` 走 `react-markdown` +
`remark-gfm` 渲染、`agent:error`/`task:error`/`error` 错误条、`agent:status` 状态栏、
回滚提示条。**本任务是补齐 hiclaw 模式下的缺口，不是重写这个组件。**

**2. 真正的阻塞在桥侧，而且不依赖任何凭据。**
`packages/hiclaw-bridge/src/index.ts` 目前是 echo 骨架：收到 `task:create` 后只回一条
`bridge:echo`，**从不发 `task:plan` / `agent:stream` / `agent:output`**。所以现在把前端
切到 hiclaw 模式，界面必然一片空白——不是前端 bug。

同目录下的 `mock-player.ts`（已完成，407 行）导出了两个函数，1:1 复刻了 legacy 在
`MOCK_LLM` 模式下的完整事件流：

```ts
export async function playTask(message: string, emit: EmitFn): Promise<void>
export async function playRoundtable(config: RoundtableConfig, emit: EmitFn): Promise<void>
export type EmitFn = (event: string, payload: unknown) => void
```

它会依次发出 `task:plan` → `agent:status` → `agent:stream` → `agent:output` →
`validator:result` → 汇总 `agent:output`，与 legacy 8648 的输出逐字节一致。

**但 `index.ts` 从未 import 它。** 这就是缺口。第一步先把它接上，前端才有东西可验证。

---

## 一、任务清单

### Step 0 · 把 mock-player 接进桥（前置，必做）

文件：`packages/hiclaw-bridge/src/index.ts`

把 `task:create` 和 `roundtable:start` 两个 handler 从「回 echo」改成「调用 mock-player」，
保留 `bridge:echo` 作为连接确认。要求：

- `emit` 实现为 `(event, payload) => socket.emit(event as never, payload as never)`
- 用 try/catch 包住，失败时 `socket.emit('task:error', { message })`
- 加一个 per-socket 的「同时只跑一个任务」保护，与 legacy `packages/server/src/index.ts`
  的 `activeTasks` 逻辑对齐（重复提交时回 `task:error`）
- 保留 `bridge:heartbeat` 不动

顺带修一处不一致：`mock-player.ts` 第 272 行 `emit('error', ...)`，应改为
`emit('task:error', ...)`。原因见下面「三、事件契约注意事项」。

**验证**：`pnpm --filter @hermes/hiclaw-bridge dev`（若无 dev 脚本则先补），
用 `socket.io-client` 连 8650 发一条 `task:create`，确认能收到 `task:plan` 与
若干 `agent:stream`。

### Step 1 · 后端模式在 UI 上可见

现在 `NEXT_PUBLIC_BACKEND` 只在 `packages/web/src/lib/socket.ts` 内部生效，界面上看不出
连的是哪个后端，演示和排障时容易搞混。

- 在 `socket.ts` 导出 `export const BACKEND_MODE = BACKEND` 与 `export const BACKEND_URL = SERVER_URL`
- ChatView header 增加一枚徽章：`hiclaw · :8650` / `legacy · :8648`，配色用现有 CSS 变量
  （`--bg-secondary` / `--text-muted`），不要引入新颜色

### Step 2 · 连接态与桥事件

`useSocket()` 已暴露 `isConnected`，但 ChatView 没用。

- 未连接时：输入框与发送按钮禁用，空态文案改为「正在连接 <mode> 后端…」
- 连接断开时：在消息流顶部显示一条非阻塞提示，重连成功后自动消失
- 订阅 `bridge:echo`，把 `message` 作为一条系统提示渲染（样式区别于 agent 气泡），
  用来在演示时肉眼确认桥已就绪。`bridge:heartbeat` 不要渲染，最多用于更新连接指示灯

**类型注意**：`bridge:echo` / `bridge:heartbeat` 不在 `@hermes/shared` 的
`ServerToClientEvents` 里，而 `useSocket` 的 `on/off` 签名被约束在该类型上。
**不要为此去改 `@hermes/shared`**（迁移期该契约冻结，legacy 与 bridge 共用）。
在 `packages/web/src/lib/events.ts` 里定义 bridge 扩展类型，并在 ChatView 内通过
`socket` 实例直接订阅这两个事件即可（`useSocket()` 已经把 `socket` 返回出来了）。

### Step 3 · 补齐 task:plan 卡片信息

桥发出的 `task:plan` 载荷比 shared 契约多两个字段：

```ts
{ tasks: SubTask[]; reasoning: string; source: 'llm' | 'rules' }
```

ChatView 现在只读 `tasks`。请补上：

- `source` 渲染为徽章：`LLM 规划` / `规则拆解`——这是评审时的加分点，能直观说明
  Planner 走的是模型还是兜底路径
- `reasoning` 折叠展示，默认收起
- 两者都要按可选字段处理（legacy 8648 的 `task:plan` 不带这两个字段，不能因此崩溃）

### Step 4 · validator:result 四维评分

`mock-player` 会发 `validator:result`，ChatView 目前完全没监听，这是 A-04 与演示叙事
里明确要展示的内容。

- 新建 `packages/web/src/components/chat/ValidatorCard.tsx`
- 渲染 `scores` 的四个维度（accuracy / completeness / safety / format），用横向条形
  或百分比数字，pass=true 用 `--success`、false 用 `--error`
- 不通过时展示 `failCodes` 与 `issues`
- 按 `taskId` 插入到对应 agent 输出之后

### Step 5 · 流式渲染的两个已知问题

- **Markdown 抖动**：流式过程中 `content` 每 6 字符变一次，`ReactMarkdown` 每次全量重解析。
  给流式中的气泡加节流（约 80–120ms 合并一次渲染），流结束后再做一次完整渲染
- **自动滚动**：现有 `bottomRef.scrollIntoView` 在每条消息变化时都触发，用户上滑查看历史
  时会被强制拽回底部。改为「仅当用户已处于底部附近（<80px）时才自动滚动」

---

## 二、验收标准

必须逐条实际跑通，不接受「代码看起来对」：

```bash
# 终端 1
pnpm --filter @hermes/hiclaw-bridge dev          # 8650

# 终端 2
NEXT_PUBLIC_BACKEND=hiclaw pnpm dev:web
```

1. 打开页面 → header 显示 `hiclaw · :8650` 徽章，连接指示为已连接
2. 收到 `bridge:echo` 的系统提示
3. 输入「生成一份新能源行业战略分析周报」并发送 →
   - 出现任务卡片，含 4 个子任务、`规则拆解` 徽章、可展开的 reasoning
   - 四个 agent 依次流式输出，文字逐段出现且无明显抖动
   - 流结束后 Markdown 正常渲染（标题、列表、表格）
   - 出现 Validator 四维评分卡
   - 最后出现汇总输出
4. 上滑到历史位置 → 新消息到达时视图不被强制拽到底部
5. 停掉桥进程 → 界面显示断连提示且输入禁用；重启桥 → 自动重连并恢复
6. 切回 `NEXT_PUBLIC_BACKEND=legacy` + `pnpm dev:server` → 行为与改动前一致（回归）

---

## 三、事件契约注意事项

**`error` 事件是死的，不要再用。** `main` 上的 `54ed078` 为规避 Socket.IO v4 客户端保留
事件，已把任务失败改发 `task:error`、圆桌失败改发 `roundtable:error`，两者现已写入
`@hermes/shared` 的 `ServerToClientEvents`。ChatView 目前同时监听新旧两个名字（兼容期），
新代码一律用 `task:error` / `roundtable:error`。

`mock-player.ts:272` 仍在发 `'error'`，Step 0 里一并改掉。

---

## 四、硬性约束

1. **`packages/web/AGENTS.md` 要求：这个 Next.js 版本有 breaking changes，写任何代码前
   先读 `node_modules/next/dist/docs/` 里的对应指南，注意废弃提示。** 不要凭训练数据里的
   Next.js 习惯下笔。
2. **不要修改 `@hermes/shared`。** 迁移期该契约冻结，legacy server 与 hiclaw bridge 共用，
   改动会同时影响两侧。前端专用的派生类型放 `packages/web/src/lib/events.ts`。
3. **不要动 `packages/server`（legacy）。** 它是红线回退路径，必须保持可用。
4. **不要引入新的 UI 依赖。** 复用现有的 `lucide-react`、`framer-motion`、
   `react-markdown`、`remark-gfm`、`@/components/ui/*`。
5. **样式一律走 CSS 变量**（`--bg-card` / `--text-primary` / `--accent-primary` /
   `--success` / `--error` 等），不要硬编码颜色——暗色模式依赖这套变量。
6. 完成后跑 `pnpm --filter @hermes/web build` 确认通过。

---

## 五、交付

- 分支 `feat/t2.2-chatview-hiclaw`，提交信息说明改了哪些事件通路
- PR 描述里贴上「二、验收标准」逐条的实际执行结果（截图或终端输出）
- 若 Step 0 的桥接线与他人工作冲突，先在 PR 里说明，不要自行改 orchestrator
